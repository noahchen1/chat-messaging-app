import { useContext, createContext, useState } from 'react';

const ContactsContext = createContext();

export default function ContactsProvider() {
    const createContact()

    return (
        <div>ContactsProvider</div>
    )
}
